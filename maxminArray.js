const numbers = [5, 2, 9, 1, 5, 6, 3];

// หาเลขที่มากที่สุดในอาร์เรย์
const maxNumber = Math.max(...numbers);

// หาเลขที่น้อยที่สุดในอาร์เรย์ 
const minNumber = Math.min(...numbers);

console.log("เลขที่มากที่สุด:", maxNumber); 
console.log("เลขที่น้อยที่สุด:", minNumber); 


